---
- hosts: all
  become: yes
  tasks:
   - name: i want to ping all nodes
     ping:
  
   - name: i want to install apache web server on my ubuntu
     apt:
       name: apache2 
       state: present
    
   - name: i want to install tree
     apt:
      name: tree